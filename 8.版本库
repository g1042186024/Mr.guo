本地版本库：存储在你电脑上的 .git目录。
远程版本库：托管在服务器（如 GitHub、GitLab）上的共享版本库。
命令：	
git commit -m "提交信息"
（保存版本，只会提交暂存区的文件，将文件提交刀版本库，如果不使用-m，就需要学习vim编辑器的操作，手动输入提交信息，该情况只适用于需要写详细的提交说明）
补充：经过我的一番查阅，原来commit会把暂存区的文件提交到版本库，带了-m参数就需要填写修改说明，该命令不只一个参数
